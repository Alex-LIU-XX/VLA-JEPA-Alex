# VLA-JEPA Piper 机器人训练启动脚本

## 配置文件（`scripts/config/piper_test.yaml`）

训练前根据实际情况修改以下关键参数：

### 数据参数

```yaml
datasets:
  vla_data:
    data_root_dir: /share/home/.../pick_place_block_all_0408_1_offset_state_v2_1_test_small   # 数据集路径
    data_mix: piper_pick_place            # mixtures.py 中注册的数据混合名
    per_device_batch_size: 8              # 【关键】每张 GPU 的 batch size
    with_state: true                      # 是否使用 proprioceptive state
    resolution_size: 224                  # VLM 图像输入分辨率
    video_resolution_size: 256            # 世界模型视频分辨率
```

### 训练参数

```yaml
trainer:
  max_train_steps: 30000                  # 【关键】总训练步数
  save_interval: 10000                    # 【关键】每多少步保存一次权重
  eval_interval: 100                      # 每多少步评估一次
  logging_frequency: 10                   # 每多少步打印日志
  num_warmup_steps: 5000                  # 学习率预热步数
  gradient_accumulation_steps: 1          # 【关键】梯度累积步数
  learning_rate:
    base: 3.0e-05                         # VLM backbone LR
    qwen_vl_interface: 1.0e-05            # VLM 接口层 LR
    action_model: 1.0e-04                 # 动作头 LR
  freeze_modules: ''                      # 冻结模块（qwen_vl / action_model / vj_encoder / vj_predictor）
  #pretrained_checkpoint: /path/to/ckpt   # 加载 Co-training 权重（可选）
```

### Batch Size 控制详解

| 参数 | 位置 | 作用 |
|---|---|---|
| `per_device_batch_size` | YAML：`datasets.vla_data.per_device_batch_size` | 每张 GPU 每次前向传播的样本数 |
| `gradient_accumulation_steps` | YAML：`trainer.gradient_accumulation_steps` | 梯度累积步数 |
| `--num_processes` | accelerate 启动参数 | GPU 数量 |

**总 batch size 公式**：
```
总 batch size = per_device_batch_size × num_processes × gradient_accumulation_steps
```

**不同配置下的总 batch size 参考**：

| GPU 数 | per_device_batch_size | gradient_accumulation_steps | 总 batch size |
|---|---|---|---|
| 1 | 2 | 1 | 2（测试用）|
| 4 | 4 | 1 | 16 |
| 4 | 8 | 1 | 32（推荐）|
| 4 | 8 | 2 | 64 |
| 8 | 8 | 1 | 64 |

> ⚠️ 如果遇到 OOM（显存不足），优先调小 `per_device_batch_size`，其次增加 `gradient_accumulation_steps`。

---

### 参数调节频率参考

**每次训练基本都要改**：

| 参数 | 行号 | 说明 |
|---|---|---|
| `max_train_steps` | 66 | 总训练步数（测试 50，正式 30000~50000） |
| `per_device_batch_size` | 59 | 每 GPU batch size（1 GPU 测试用 2，4 GPU 正式用 8） |
| `save_interval` | 68 | 权重保存间隔（需 < `max_train_steps` 才会保存） |
| `data_root_dir` | 54 | 数据集路径 |
| `data_mix` | 55 | `mixtures.py` 中注册的 key |

**偶尔调**：

| 参数 | 行号 | 说明 |
|---|---|---|
| `learning_rate.base` | 71 | VLM backbone 学习率（默认 3e-5） |
| `learning_rate.action_model` | 73 | 动作头学习率（默认 1e-4，动作头通常比 VLM 学得快） |
| `freeze_modules` | 77 | 冻结模块加速训练，如 `qwen_vl`（只训练动作头） |
| `gradient_accumulation_steps` | 86 | 梯度累积步数（显存不足时增大，等效减小实际 batch） |
| `num_warmup_steps` | 67 | LR 预热步数（正式训练通常 5000） |
| `eval_interval` | 69 | 评估间隔（每 N 步算一次 MAE/MSE） |
| `num_frames` | 48 | V-JEPA 输入视频帧数（显存不足可改 4） |

**基本不动**：

| 参数 | 说明 |
|---|---|
| `action_dim` / `state_dim` | 取决于机器人，改一次即可 |
| `future_action_window_size` / `action_horizon` | 预测窗口大小 |
| `loss_scale.vla` / `loss_scale.vlm` | action loss 和 world model loss 权重 |
| `noise_beta_alpha` / `noise_beta_beta` | Flow Matching Beta 分布参数 |
| `attn_implementation` | `flash_attention_2`（GPU 不支持时改 `eager`） |

---

## 1. 测试模式（1 GPU，快速验证数据加载和模型能否跑通）

```bash
cd /share/home/tm866052366100000/a926312360/LXX/project/VLA-JEPA && \
CUDA_VISIBLE_DEVICES=0 /opt/conda/envs/VLA_JEPA/bin/accelerate launch \
  --config_file ./starVLA/config/deepseeds/accelerate_test.yaml \
  --main_process_port 29504 \
  ./starVLA/training/train_starvla.py \
  --config_yaml ./scripts/config/piper_test.yaml
```

> 测试配置文件 `piper_test.yaml` 中 `max_train_steps: 50`、`per_device_batch_size: 2`，仅验证流程，不保存权重。

---

## 2. 正式训练（4 GPU）

```bash
cd /share/home/tm866052366100000/a926312360/LXX/project/VLA-JEPA && \
CUDA_VISIBLE_DEVICES=0,1,2,3 /opt/conda/envs/VLA_JEPA/bin/accelerate launch \
  --config_file ./starVLA/config/deepseeds/deepspeed_zero2.yaml \
  --num_processes 4 \
  ./starVLA/training/train_starvla.py \
  --config_yaml ./scripts/config/piper_test.yaml
```

> 正式训练前请先修改 `piper_test.yaml`：
> - `max_train_steps: 30000`
> - `per_device_batch_size: 8`（4 GPU 时总 batch = 32）
> - `save_interval: 10000`

---

## 3. 训练输出目录

```
checkpoints/piper_test/
├── config.yaml                     # 本次训练的配置
├── config.json
├── dataset_statistics.json         # 数据集统计信息
├── tensorboard/                    # TensorBoard 日志
├── steps_10000_pytorch_model.pt    # 第 10000 步权重
├── steps_20000_pytorch_model.pt    # 第 20000 步权重
├── steps_30000_pytorch_model.pt    # 第 30000 步权重
└── final_model/
    └── pytorch_model.pt            # 训练完成最终权重
```

---

## 4. 多 GPU 注意事项

- `--num_processes` 需与 `CUDA_VISIBLE_DEVICES` 中的 GPU 数量一致
- 4 × RTX 4090 (24GB) 推荐 `per_device_batch_size=8`，总 batch size = 32
- 如遇 OOM，调小 `per_device_batch_size` 或增大 `gradient_accumulation_steps`
- 如需冻结模块加速训练，设置 `freeze_modules: qwen_vl`（冻结 VLM 骨干，只训练动作头）
